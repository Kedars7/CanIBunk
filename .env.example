# Backend Server Configuration
PORT=7000

# MongoDB Database Connection
# Replace with your MongoDB Atlas connection string or local MongoDB URL
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname?retryWrites=true&w=majority

# JWT Secret Key for Authentication
# Generate a secure random string for production (use: openssl rand -base64 32)
JWT_SECRET_KEY=your_super_secret_jwt_key_change_this_in_production

# Frontend URL for CORS Configuration
# ⚠️ IMPORTANT: Set this to your actual FRONTEND URL (e.g., Netlify, Vercel)
# For Netlify: https://your-app-name.netlify.app
# For Vercel: https://your-app-name.vercel.app
# NOT the backend URL (Render)!
FRONTEND_URL=https://your-frontend-url.com
